# ChatGPT pass: real visuals for the landing page

Follow-up to `chatgpt-brand/` (which chose the brand mark and rebuilt the page structure). This round only adds illustration, icons, diagrams and imagery on top of what's already approved — it does not change structure or copy.

- `PROMPT.md` — paste into ChatGPT and attach every file in `reference/` (both HTML files, COPY-RULES.md, the three brand SVGs).
- `reference/` — the live page as of 2026-09-22 (https://puteramohammadnilam-cmd.github.io/campaign-grid-landing/), the copy rules, the approved brand mark.
- `output/` — ChatGPT writes here: the two revised HTML files plus an `assets/` folder for anything new.

After ChatGPT delivers: Claude reviews against the rules above, tests in a browser (both languages, phone width, images-off, keyboard nav, all six tabs, the approval toggle, the form), then copies the result into `landing-pages/` and `docs/` and republishes — same process as the last two rounds. Nothing goes live without that review.
